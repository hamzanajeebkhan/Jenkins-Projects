## Setting up Jenkins locally or in Production using Docker

### Requirements / Pre-requisites
- Docker
- Docker-Compose

### Method
- Run below command to start the project

```
docker compose -f docker-compose.yml up -d
```